;; The function a-plus-abs-b selects the correct operator to use in order to add the
;; absolute value of b to a. That is, if b is less than zero be is subtracted from a,
;; otherwise it is added to a.
